{-# LANGUAGE OverloadedStrings #-}

module Testtry where


import Reflex.Dom.Core
-- (text, display, count, elAttr, (=:), (&), mainWidget, MonadWidget, foldDyn, Dynamic)
import Data.Monoid ((<>))
-- import Language.Javascript.JSaddle (JSM)

import Reflex.Dom.HTML5.Attrs
-- as A (Globals, AnyAttr, id_, URL(URL), className,
--                                    placeholder, srcDoc, src, width, href,
--                                    sbAllowSameOrigin, title, attrMap,
--                                    valueOlLi, gDef, reversed)
import Reflex.Dom.HTML5.Elements
-- as E (eInput, eEmbedC, eIFrameC, eBN, eA,
--                                       eP, eAC, ePN, eH1N, eDivN, eUlN, eLiN,
--                                       eLi, eOlN, eOl, eBr_, eH2N)


